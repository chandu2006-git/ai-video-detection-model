services:
  - type: web
    name: ai-video-detection-lab

    env: python

    buildCommand: |
      pip install -r requirements.txt

    startCommand: |
      uvicorn backend.main:app --host 0.0.0.0 --port $PORT